import Topbar from '@/components/topbar/Topbar'
import React from 'react'

type Props = {}

const Home = (props: Props) => {
  return (
    <div>
        <Topbar />
    </div>
  )
}

export default Home